MaterialApp() 	
//Makes a material app so that we can use the Material widgets

Scaffold() 	
//Provides a default app bar, title, and a body property that holds the widget tree for the home screen

AppBar()		
//Inserts an app bar 

Text() 		
//For inserting text

Center()		
//aligns the child to the center

/*
Stateless widgets
are immutable, meaning that their properties can’t change
(i.e. all values are final)
Stateful widgets 	maintain state that might change during the lifetime of the widget
requires a (1) StatefulWidget class that creates an instance of (2) a State class (e.g. RandomWordsState)
*/

EdgeInsets.all(double value)
